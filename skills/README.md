# Skills Index

> Auto-generated by `scripts/sync-skills.sh` from `plugins/*/skills/*/SKILL.md`.
> **Do not edit files here.** Edit the canonical source in `plugins/` and re-run the sync.

| Plugin | Skill | Description |
|---|---|---|
| `agent-workflows` | [`bootstrap-repo-agent`](./agent-workflows__bootstrap-repo-agent/SKILL.md) | Bootstrap agent infrastructure for a repository - triage label config, stakeholders, and local companion skill stubs. Run once when setting up agent workflows on a new or existing repository. |
| `agent-workflows` | [`check-impl-against-spec`](./agent-workflows__check-impl-against-spec/SKILL.md) | Compare a pull request's implementation against approved spec context and fold material mismatches into review.json. Use during PR review when product or tech specs exist under specs/. |
| `agent-workflows` | [`convention-review`](./agent-workflows__convention-review/SKILL.md) | Review code changes against project conventions - middleware patterns, import rules, auth flow, validation, logging, response helpers, IaC patterns, and frontend stack compliance. Use when asked to review code for convention adherence, audit a PR, or spot-check patterns. |
| `agent-workflows` | [`implement-issue`](./agent-workflows__implement-issue/SKILL.md) | Implement a GitHub issue with spec awareness, keeping product and tech specs aligned with code as implementation evolves. Use when assigned an issue to implement, or when asked to build a feature described in a GitHub issue. |
| `agent-workflows` | [`review-pr`](./agent-workflows__review-pr/SKILL.md) | Review a pull request diff and write structured feedback to review.json with severity labels, suggestion blocks, and a verdict. Use when reviewing a checked-out PR or when asked to review code changes against a base branch. |
| `agent-workflows` | [`security-review`](./agent-workflows__security-review/SKILL.md) | Adversarial security review of code changes - PII handling, secrets usage, IAM scoping, authentication flow integrity, injection vectors, and cross-tenant leaks. Use when reviewing a PR with security-sensitive changes, before a production deploy, or when auditing a specific concern. |
| `agent-workflows` | [`triage-issue`](./agent-workflows__triage-issue/SKILL.md) | Triage a GitHub issue by analyzing the report, inspecting relevant code, estimating reproducibility, detecting duplicates, and writing structured output to triage_result.json. Use when triaging a new or reopened issue, or when asked to classify and label an issue. |
| `agent-workflows` | [`write-product-spec`](./agent-workflows__write-product-spec/SKILL.md) | Write a PRODUCT.md-style spec for a significant feature, focused on user-facing behavior, invariants, edge cases, and validation. Use when the user asks for a product spec, PRD, or behavior doc, or when a feature is substantial enough that a written spec would improve implementation quality. |
| `agent-workflows` | [`write-tech-spec`](./agent-workflows__write-tech-spec/SKILL.md) | Write a TECH.md-style spec for a significant feature grounded in actual codebase patterns, documenting architecture, tradeoffs, and validation. Use when the user asks for a technical spec, implementation plan, or architecture doc tied to a product spec. |
| `aws-serverless-patterns` | [`apigateway-websocket`](./aws-serverless-patterns__apigateway-websocket/SKILL.md) | Build realtime features with API Gateway WebSocket APIs - route keys, connection management in DynamoDB, tenant-scoped broadcast, reconnection handling, and keep-alive. Use when adding a WebSocket route, implementing realtime updates, or debugging dropped connections. |
| `aws-serverless-patterns` | [`cdk-nested-stack`](./aws-serverless-patterns__cdk-nested-stack/SKILL.md) | Structure an AWS CDK app using parent + nested stacks - a parent Stack that composes feature-scoped NestedStacks via props, cross-stack references, and deploy-ordered outputs. Use when a single stack is getting too large or when features should be deployable in isolation. |
| `aws-serverless-patterns` | [`cloudfront-spa`](./aws-serverless-patterns__cloudfront-spa/SKILL.md) | Host an SPA on CloudFront + S3 with OAC, SPA rewrite (404 → index.html), cache policies for hashed vs. HTML assets, WAF attachment, and security headers. Use when adding a new distribution, debugging stale assets, or tuning cache. |
| `aws-serverless-patterns` | [`codebuild-buildspec`](./aws-serverless-patterns__codebuild-buildspec/SKILL.md) | Author or debug a CodeBuild buildspec.yml with phase conventions, caching, artifacts, secret sourcing, and common failure modes. Use when editing a buildspec, debugging a failed build, adding a new CI job, or tuning build time. |
| `aws-serverless-patterns` | [`dynamodb-single-table`](./aws-serverless-patterns__dynamodb-single-table/SKILL.md) | Design, query, or extend a DynamoDB single-table with partition/sort key conventions, GSI reuse strategy, entity schemas, and multi-tenant isolation. Use when adding a new entity or access pattern, debugging a DDB query, or evaluating whether a new GSI is needed. |
| `aws-serverless-patterns` | [`dynamodb-streams-fanout`](./aws-serverless-patterns__dynamodb-streams-fanout/SKILL.md) | Fan out DynamoDB Stream events to multiple downstream consumers - search sync, webhooks, notifications, audit trails - with entity-prefix filtering, idempotent handlers, and error isolation. Use when adding a new stream consumer or debugging a broken downstream sync. |
| `aws-serverless-patterns` | [`elasticache-valkey`](./aws-serverless-patterns__elasticache-valkey/SKILL.md) | Use ElastiCache Valkey (Redis-compatible) for caching, rate limiting, and idempotency in serverless applications. Covers VPC connectivity, key conventions, TTL defaults, and atomic operations. Use when a Lambda needs caching, rate limiting, or distributed locking. |
| `aws-serverless-patterns` | [`flyway-migrations`](./aws-serverless-patterns__flyway-migrations/SKILL.md) | Author or review a Flyway SQL migration with version naming, idempotent patterns, PostgreSQL dialect, and destructive migration safeguards. Use when adding a new migration, reviewing a PR that touches db/, or debugging a failed migration. |
| `aws-serverless-patterns` | [`middy-lambda-authoring`](./aws-serverless-patterns__middy-lambda-authoring/SKILL.md) | Scaffold a middy-wrapped AWS Lambda handler with JSON Schema validation, CORS, HTTP error handling, and Powertools structured logging. Use when creating a new Node.js Lambda handler behind API Gateway. |
| `aws-serverless-patterns` | [`opensearch-sync`](./aws-serverless-patterns__opensearch-sync/SKILL.md) | Sync DynamoDB data into OpenSearch for full-text search via Streams, with per-tenant index isolation, query patterns, and sync lag handling. Use when querying OpenSearch from a Lambda, adding an indexed entity, or debugging sync lag. |
| `aws-serverless-patterns` | [`powertools-logger`](./aws-serverless-patterns__powertools-logger/SKILL.md) | Wire AWS Lambda Powertools Logger (Node.js) into a handler via the middy middleware, with structured logging, correlation ids, sampling, and POWERTOOLS_SERVICE_NAME env var. Use when adding structured logging to a new Lambda or migrating from console.log. |
| `aws-serverless-patterns` | [`sam-api-gateway`](./aws-serverless-patterns__sam-api-gateway/SKILL.md) | Author a SAM template fronting Lambda handlers with API Gateway REST API - events, CORS, stages, authorizers, and esbuild build. Use when adding a new HTTP endpoint in a SAM project or wiring a new API Gateway from scratch. |
| `aws-serverless-patterns` | [`secrets-manager-patterns`](./aws-serverless-patterns__secrets-manager-patterns/SKILL.md) | Manage AWS Secrets Manager with path conventions, IAM policy scoping, in-Lambda caching, and rotation strategies. Use when adding a new secret, scoping a Lambda's IAM policy, or designing a secret path hierarchy. |
| `aws-serverless-patterns` | [`ses-notifications`](./aws-serverless-patterns__ses-notifications/SKILL.md) | Build transactional notification systems with SES templated emails, SNS topic conventions, and EventBridge Scheduler for delayed or recurring sends. Use when adding a new notification, wiring an SES template, scheduling a reminder, or debugging missed sends. |
| `aws-serverless-patterns` | [`step-functions-workflow`](./aws-serverless-patterns__step-functions-workflow/SKILL.md) | Author or debug an AWS Step Functions state machine with ASL conventions, retry and error-handler patterns, service integrations, and CDK/SAM wiring. Use when creating a new workflow, adding a state to an existing machine, or debugging a failing execution. |
| `aws-serverless-patterns` | [`waf-baseline`](./aws-serverless-patterns__waf-baseline/SKILL.md) | Configure AWS WAF with managed rule groups, rate-based rules, IP allow/deny sets, and scope-down statements. Use when adding a new rule, tuning rate limits, debugging a blocked request, or setting up WAF for a new API or distribution. |
| `bubbletea-cli-kit` | [`bubbletea-form-view`](./bubbletea-cli-kit__bubbletea-form-view/SKILL.md) | Implement a multi-field form in Bubbletea v2 with focus management, per-field validation, and submit/cancel flow. Use when the user wants a TUI form to collect multiple inputs (new PR, issue template, config setup, etc.). |
| `bubbletea-cli-kit` | [`bubbletea-list-view`](./bubbletea-cli-kit__bubbletea-list-view/SKILL.md) | Implement a selectable list view in Bubbletea v2 with filter input, keybindings (j/k, arrow keys, /, enter), and a status bar footer. Use when the user wants a menu, picker, or scrollable selection interface in a Go TUI. |
| `bubbletea-cli-kit` | [`bubbletea-scaffold`](./bubbletea-cli-kit__bubbletea-scaffold/SKILL.md) | Scaffold a new Go TUI CLI using Bubbletea v2, Bubbles, and Lipgloss with a clean layout (cmd/ + internal/tui/ + Makefile). Use when the user wants a new Go CLI, a terminal UI, or a Bubbletea project. |
| `bubbletea-cli-kit` | [`bubbletea-test`](./bubbletea-cli-kit__bubbletea-test/SKILL.md) | Test a Bubbletea v2 TUI model by driving Update with synthetic messages (pure, fast) and snapshotting View output for regression. Use when adding tests for a Bubbletea model, debugging a flaky UI, or setting up the test harness for a new CLI. |
| `database-patterns` | [`database-selection`](./database-patterns__database-selection/SKILL.md) | Decision framework for choosing the right database paradigm — relational (PostgreSQL/MySQL), document (MongoDB), graph (Neo4j), key-value (Redis/Valkey), or columnar (DuckDB). Covers OLTP vs OLAP, ACID tradeoffs, read-heavy vs write-heavy workloads, and caching considerations. Use when starting a new project, evaluating data storage options, or deciding whether to add a supplementary database. |
| `database-patterns` | [`duckdb-analytics`](./database-patterns__duckdb-analytics/SKILL.md) | DuckDB columnar OLAP patterns — reading from Parquet/CSV/S3, GROUP BY ALL, COPY TO for export, OLAP vs OLTP distinction, ETL/ELT/Reverse-ETL concepts, and when columnar databases are the right choice. Use when running analytics, building dashboards, exploring large datasets, or evaluating whether an OLAP database is needed. |
| `database-patterns` | [`mongodb-querying`](./database-patterns__mongodb-querying/SKILL.md) | MongoDB query patterns — find/findOne, query operators ($gt/$lt/$in/$ne/$eq), logical operators ($and/$or/$not/$nor), sorts, projections, limits, cursors, and text search with $text. Best fit for unstructured or variably-shaped document data. Use when writing MongoDB queries, building API endpoints that read from Mongo, or debugging query results. |
| `database-patterns` | [`mongodb-updates-aggregation`](./database-patterns__mongodb-updates-aggregation/SKILL.md) | MongoDB write operations and aggregation — insertOne/Many, updateOne/Many with $set/$inc/$unset, upsert, deleteOne/Many, bulkWrite, replaceOne, findOneAndUpdate, and aggregation pipelines ($match, $bucket, $sort, $group stages). Use when writing data to MongoDB, building batch operations, or deriving analytical insights from collections. |
| `database-patterns` | [`neo4j-cypher`](./database-patterns__neo4j-cypher/SKILL.md) | Neo4j graph database patterns with Cypher query language — CREATE nodes and relationships, MATCH patterns with WHERE filters, variable-length paths, shortestPath, UNWIND, constraints, indexes, and EXPLAIN. Best for data defined by multi-hop relationships (social graphs, recommendation engines, fraud detection). Use when modeling or querying graph data, or evaluating whether a graph DB is needed. |
| `database-patterns` | [`postgresql-indexing`](./database-patterns__postgresql-indexing/SKILL.md) | PostgreSQL index types and query performance — EXPLAIN for query planning, B-tree indexes, compound indexes, unique indexes, GIN indexes for full-text search with tsvector/tsquery, and when to create (or avoid) indexes. Use when a query is slow, when adding a new table, or when setting up full-text search. |
| `database-patterns` | [`postgresql-querying`](./database-patterns__postgresql-querying/SKILL.md) | PostgreSQL query patterns — SELECT with projections, WHERE filters, JOINs (INNER/LEFT/RIGHT/FULL OUTER), subqueries, GROUP BY, ORDER BY, JSONB querying with bracket notation and ->> operator, and parameterized queries to prevent SQL injection. Best fit for relational data with structured schemas that benefit from cross-table relationships. Use when writing or reviewing SQL queries, building API endpoints that read from Postgres, or debugging slow queries. |
| `database-patterns` | [`postgresql-vector-search`](./database-patterns__postgresql-vector-search/SKILL.md) | Vector search with pgvector for RAG (retrieval-augmented generation) — embedding models (Ollama nomic-embed-text, OpenAI text-embedding-3-small), storing vectors in PostgreSQL, the <=> cosine distance operator, and when RAG helps or hurts. Use when building RAG pipelines, adding semantic search, or evaluating whether vector search is needed. |
| `database-patterns` | [`redis-patterns`](./database-patterns__redis-patterns/SKILL.md) | Redis/Valkey patterns — SET/GET, colon-namespaced key naming, INCR/DECR counters, TTL with EX/PX, NX/XX conditional writes, data types (strings, lists, hashes, sets, sorted sets), caching strategies, thundering herd mitigation, and LRU eviction. Use when implementing caching, rate limiting, session storage, counters, or evaluating whether a cache layer is needed. |
| `homelab-ops` | [`docker-compose-baseline`](./homelab-ops__docker-compose-baseline/SKILL.md) | Author a docker-compose.yml for a self-hosted service with healthchecks, resource limits, restart policies, named volumes, user bridge networks, and explicit logging. Use when adding a new service to a homelab compose file or hardening an existing one. |
| `homelab-ops` | [`podman-quadlet`](./homelab-ops__podman-quadlet/SKILL.md) | Declarative container management via Podman Quadlet - .container files in /etc/containers/systemd/ that systemd translates into rootless service units. Use when running containers on Ubuntu / RHEL / Fedora without Docker, or when you want containers to run as first-class systemd services. |
| `homelab-ops` | [`ssh-hardening`](./homelab-ops__ssh-hardening/SKILL.md) | Harden SSH on a Linux server - disable password auth, require keys, disable root login, configure fail2ban, and decide whether to change the port. Use when securing SSH on a new server or auditing an existing one. |
| `homelab-ops` | [`systemd-service-authoring`](./homelab-ops__systemd-service-authoring/SKILL.md) | Write a production-grade systemd unit file with Restart policy, hardening (NoNewPrivileges, ProtectSystem), user sandboxing, and journal logging. Use when the user wants to run a long-running process as a service on Linux. |
| `homelab-ops` | [`ufw-basics`](./homelab-ops__ufw-basics/SKILL.md) | Configure ufw (Uncomplicated Firewall) on Ubuntu / Debian with a default-deny baseline, essential allows, logging, and safe enable sequence that won't lock you out over SSH. Use when setting up the first firewall rules on a new server or auditing an existing ruleset. |
| `homelab-ops` | [`unattended-upgrades`](./homelab-ops__unattended-upgrades/SKILL.md) | Configure unattended-upgrades on Ubuntu / Debian for automatic security patching, including reboot policy, email notifications, and package exclusions. Use when setting up a headless server that should patch itself safely. |
| `mcp-authoring` | [`mcp-publish`](./mcp-authoring__mcp-publish/SKILL.md) | Checklist to submit an MCP server to the official MCP Registry using an io.github.<owner>/<server> namespace. Use when the user is ready to publish an MCP server publicly. |
| `mcp-authoring` | [`mcp-scaffold-python`](./mcp-authoring__mcp-scaffold-python/SKILL.md) | Scaffold a new stdio MCP server in Python using FastMCP. Use when the user wants a Python MCP server, is in a Python-heavy project, or wants to expose Python code to Claude. |
| `mcp-authoring` | [`mcp-scaffold-ts`](./mcp-authoring__mcp-scaffold-ts/SKILL.md) | Scaffold a new stdio MCP server in TypeScript using @modelcontextprotocol/sdk and Zod. Use when the user says they want to build an MCP server, wrap a tool as MCP, or expose TS code to Claude/other MCP hosts. |
| `mcp-authoring` | [`mcp-wrap-cli`](./mcp-authoring__mcp-wrap-cli/SKILL.md) | Wrap an existing CLI (Go, Rust, Bash, Node, Python) as an MCP server by mapping its subcommands to MCP tools. Use when the user has a CLI they already trust and want agents to invoke it. |
| `nextjs-app-router-kit` | [`nextjs-route-scaffold`](./nextjs-app-router-kit__nextjs-route-scaffold/SKILL.md) | Scaffold a Next.js 16 App Router route segment (page.tsx + loading + error + layout) using Server Components by default. Use when the user wants to add a new route or page in a Next.js project. |
| `nextjs-app-router-kit` | [`nextjs-server-action`](./nextjs-app-router-kit__nextjs-server-action/SKILL.md) | Author a Next.js 16 Server Action with auth check, Zod-validated input, a discriminated-union return shape ({ ok: true, data } \| { ok: false, error }), and route revalidation. Use when adding a form submission handler or server mutation in a Next.js App Router project. |
| `nextjs-app-router-kit` | [`shadcn-component-add`](./nextjs-app-router-kit__shadcn-component-add/SKILL.md) | Add a shadcn/ui component to a Next.js 16 + Tailwind 4 project via the shadcn CLI, and wire it with design tokens defined in app/globals.css @theme. Use when scaffolding a new shadcn component (button, dialog, input, etc.) or customizing one that was previously generated. |
| `self-improvement` | [`respond-to-feedback`](./self-improvement__respond-to-feedback/SKILL.md) | Handle @oz-agent mentions in GitHub issue and PR comments. Parse the feedback, classify it, and route to the appropriate workflow - skill improvement, issue triage, PR review, or implementation. Use when triggered by a GitHub Actions workflow that detected an @oz-agent mention. |
| `self-improvement` | [`self-improve-skills`](./self-improvement__self-improve-skills/SKILL.md) | Analyze recent agent performance evidence (closed issues, merged PRs, maintainer overrides, feedback comments) and propose updates to local companion skills via a pull request. Use on a weekly schedule, after a batch of agent-driven work, or when asked to improve agent skills from accumulated evidence. |
